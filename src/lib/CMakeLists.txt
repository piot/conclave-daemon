# generated by cmake-generator
cmake_minimum_required(VERSION 3.16.3)

add_executable(conclave-daemon 
  daemon.c
  main.c)

include(Tornado.cmake)
set_tornado(conclave-daemon)

target_include_directories(conclave-daemon PRIVATE ../include)


target_link_libraries(conclave-daemon PUBLIC 
  conclave-server-lib
  udp-server
  guise-client-udp)

